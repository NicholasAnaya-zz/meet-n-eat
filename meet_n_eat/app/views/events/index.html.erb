<% @events.each do |event| %>
	<%= link_to event.host.first_name, account_path(event.host.id) %> is having <%= link_to "meal", event_path(event.id) %> at <%= event.spot %> <%= button_to "Join Event", join_path(event.id) %>
	<br>
<% end %>
